# API service URLs
# For local development: use http://localhost:6410 (webpack proxy handles routing)
# For Kubernetes/Cloud: leave empty or use relative path (same-origin)
# All services go through nginx proxy at localhost:6410 in local dev
# Nginx will forward to backend services using Docker service names
VITE_AUTH_SERVICE_URL=http://localhost:6410
VITE_IMAGE_SERVICE_URL=http://localhost:6410
VITE_VISION_SERVICE_URL=http://localhost:6410
VITE_RESULTS_SERVICE_URL=http://localhost:6410
VITE_LABWARE_SERVICE_URL=http://localhost:6410
VITE_PREDICTION_SERVICE_URL=http://localhost:6410

# Capture service (use same-origin proxy through nginx)
VITE_VISION_CAPTURE_SERVICE_URL=http://localhost:6410
VITE_WS_CAPTURE_URL=ws://localhost:6410/ws/capture
VITE_VISION_CAPTURE_SERVICE_HOST=localhost
VITE_VISION_CAPTURE_SERVICE_PORT=6407

# Ingress path prefixes (for Kubernetes/Cloud deployment)
# Leave empty for local development (webpack proxy handles routing)
# Set to ingress paths for Kubernetes: /auth, /result, /vision, etc.
# Example for Kubernetes:
# VITE_AUTH_PATH_PREFIX=/auth
# VITE_IMAGE_PATH_PREFIX=/ingestion
# VITE_VISION_PATH_PREFIX=/vision
# VITE_RESULTS_PATH_PREFIX=/result
# VITE_LABWARE_PATH_PREFIX=/interface
# VITE_PREDICTION_PATH_PREFIX=/prediction
# VITE_CAPTURE_PATH_PREFIX=/capture

# Shared resources
VITE_MINIO_BASE_URL=http://localhost:9000
VITE_API_BASE_URL=http://localhost:6410

# Docker / frontend port
FRONTEND_PORT=6410
